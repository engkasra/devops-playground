# Copy to .env and adjust. .env must NOT be committed.
APP_NAME=devops-demo
APP_ENV=production
LOG_LEVEL=INFO
PORT=8000

# Example of a value that would normally come from a secret store / K8s Secret.
API_TOKEN=